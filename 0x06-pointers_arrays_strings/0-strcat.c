#include "main.h"

/**
 * _strcat - function to apprend dest and src
 * @dest: destination string
 * @src: source string
 * Return: Always (0) success
 */

char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
