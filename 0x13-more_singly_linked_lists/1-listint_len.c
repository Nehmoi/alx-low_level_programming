#include "lists.h"

/**
 * listint_len - a function that returns the number of elements in a linked list
 * @n: integers elements
 *
 * Decription: singly linked list retunrs numbers of elements in list
 *
 * Return: Always (0) success
 */

size_t listint_len(const listint_t *h)
{
	return(0);
}
