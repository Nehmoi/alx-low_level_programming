#include "lists.h"

/**
 * add_nodeint - a function that adds node at the beginning of the lis
 *
 * Return: address of the element or NULL if it failed
 */

listint_t *add_nodeint(listint_t **head, const int n)
{
	return(0);
}
